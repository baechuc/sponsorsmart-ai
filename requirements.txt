# SponsorSmart AI — Requirements
# Python 3.12  (set this in Streamlit Cloud → Advanced settings)

# ── App ──────────────────────────────────────────────
streamlit>=1.32.0

# ── Deep Learning ────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.38.0,<5.0.0
huggingface_hub>=0.21.0,<2.0.0

# ── Machine Learning ─────────────────────────────────
# PINNED: svm_model.pkl was trained with scikit-learn 1.6.1.
# A newer version (1.9.x) cannot unpickle the SVC correctly
# -> AttributeError: 'SVC' object has no attribute '_effective_probability'
scikit-learn==1.6.1
imbalanced-learn>=0.12.0  # only used during training (SMOTE); harmless at inference

# ── PDF & OCR ────────────────────────────────────────
pdfplumber>=0.10.0
pdf2image>=1.16.0
pytesseract>=0.3.10
Pillow>=10.0.0

# ── NLP Bahasa Indonesia ─────────────────────────────
PySastrawi>=1.2.0
nltk>=3.8.1

# ── Data & Visualisasi ───────────────────────────────
pandas>=2.0.0
numpy>=1.26.0
matplotlib>=3.7.0
seaborn>=0.13.0
wordcloud>=1.9.0
